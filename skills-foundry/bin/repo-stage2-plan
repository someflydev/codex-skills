#!/usr/bin/env python3
import sys

from _repo_workflow import main_repo_stage2_plan


if __name__ == "__main__":
    print(
        "DEPRECATED alias: use `repo-helper-stage2-theme-plan` (helper-only stage-2 theme planning command).",
        file=sys.stderr,
    )
    raise SystemExit(main_repo_stage2_plan())
