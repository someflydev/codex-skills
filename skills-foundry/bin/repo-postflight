#!/usr/bin/env python3
import sys

from _repo_workflow import main_repo_postflight


if __name__ == "__main__":
    print(
        "DEPRECATED alias: use `repo-helper-postflight` (helper-only postflight snapshot command).",
        file=sys.stderr,
    )
    raise SystemExit(main_repo_postflight())
