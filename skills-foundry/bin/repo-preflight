#!/usr/bin/env python3
import sys

from _repo_workflow import main_repo_preflight


if __name__ == "__main__":
    print(
        "DEPRECATED alias: use `repo-helper-preflight` (helper-only prompt preflight inventory command).",
        file=sys.stderr,
    )
    raise SystemExit(main_repo_preflight())
